import styles from './Header.module.css';

const Header = () => {
    return (
        <header className={styles.header}>
            <h1>Xos gelmisen multiBanka</h1>
        </header>
    );
};

export default Header;
